# Review package: d744a677b05913c5e83bf5a57bbff4a214d7f433..c4361789022dff7807e8c26485087702f2df42a2

## Commits
c436178 fix: serialize compose dependency installation

## Files changed
 docker-compose.yml | 19 +++++++++++++++++--
 1 file changed, 17 insertions(+), 2 deletions(-)

## Diff
diff --git a/docker-compose.yml b/docker-compose.yml
index f2b8b4f..978671d 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -15,44 +15,59 @@ services:
 
   keycloak:
     image: quay.io/keycloak/keycloak:26.2
     command: start-dev
     environment:
       KC_BOOTSTRAP_ADMIN_USERNAME: admin
       KC_BOOTSTRAP_ADMIN_PASSWORD: admin_local_only
     ports:
       - "8080:8080"
 
+  workspace-dependencies:
+    image: node:22-bookworm-slim
+    working_dir: /workspace
+    command: sh -c "corepack enable && pnpm install --frozen-lockfile"
+    volumes:
+      - .:/workspace
+      - workspace-node-modules:/workspace/node_modules
+
   web:
     image: node:22-bookworm-slim
     working_dir: /workspace
-    command: sh -c "corepack enable && pnpm install --frozen-lockfile && pnpm dev"
+    command: sh -c "corepack enable && pnpm dev"
     environment:
       DATABASE_URL: postgresql://dossie:dossie_local_only@postgres:5432/dossie_triagem
       KEYCLOAK_ISSUER_URL: http://keycloak:8080
     depends_on:
       postgres:
         condition: service_healthy
       keycloak:
         condition: service_started
+      workspace-dependencies:
+        condition: service_completed_successfully
     volumes:
       - .:/workspace
+      - workspace-node-modules:/workspace/node_modules
     ports:
       - "3000:3000"
 
   worker:
     image: node:22-bookworm-slim
     working_dir: /workspace
-    command: sh -c "corepack enable && pnpm install --frozen-lockfile && pnpm worker"
+    command: sh -c "corepack enable && pnpm worker"
     environment:
       DATABASE_URL: postgresql://dossie:dossie_local_only@postgres:5432/dossie_triagem
       KEYCLOAK_ISSUER_URL: http://keycloak:8080
     depends_on:
       postgres:
         condition: service_healthy
       keycloak:
         condition: service_started
+      workspace-dependencies:
+        condition: service_completed_successfully
     volumes:
       - .:/workspace
+      - workspace-node-modules:/workspace/node_modules
 
 volumes:
   postgres-data:
+  workspace-node-modules:
